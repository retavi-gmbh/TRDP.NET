name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: |
            8.0.x
            10.0.x

      - name: Restore
        run: dotnet restore Trdp.Net.slnx

      - name: Build
        run: dotnet build Trdp.Net.slnx -c Release --no-restore

      - name: Test
        run: dotnet test Trdp.Net.slnx -c Release --no-build --verbosity normal
